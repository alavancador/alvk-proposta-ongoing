{
  "name": "alvk-proposta-ongoing",
  "version": "1.0.0",
  "private": true,
  "description": "Ambiente estático de propostas comerciais da ALVK.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "npm run build && node --test tests/*.test.mjs",
    "check": "npm test"
  }
}
